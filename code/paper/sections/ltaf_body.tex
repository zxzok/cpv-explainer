At $N=4$, each template observes \numAfObservedPctFour{} of the analysable
record, equivalent to \numAfEquivalentHoursFour{} hour on a 24-hour record.
The contiguous and dispersed templates attain cross-fitted $R^2$ values
\numCfAfContigOneHour{} and \numCfAfDispOneHour, a difference of
\numAfBootDiffFour{} with conditional paired range \numAfBootRangeFour. At
$N=16$, the fraction is \numAfObservedPctSixteen{} and the corresponding
values are \numCfAfContigFourHour{} and \numCfAfDispFourHour; their difference
is \numAfBootDiffSixteen{} with range \numAfBootRangeSixteen.

The conditional paired range has a positive lower endpoint at every evaluated
budget containing at least two windows. At $N=1$, the difference is
\numAfBootDiffOne{} with range \numAfBootRangeOne. The two single-window
templates occupy different locations, so this cell estimates a location
contrast; budgets of at least two additionally compare dispersed with
contiguous layouts. Outcome-defined cohort and grid-discretisation sensitivities
are reported in Appendix~\ref{sec:appendix-real-sensitivity}.
